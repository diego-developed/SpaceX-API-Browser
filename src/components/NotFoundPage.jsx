import React from 'react';
import { Typography } from '@mui/material';

const NotFoundPage = () => {
    return (
      <div style={{ padding: '20px' }}>
        <Typography gutterBottom variant="h6" component="div">Error (404): Page not Found.</Typography>
      </div>
    );
  }

  export default NotFoundPage;